import React from 'react';
import ReactDOM from 'react-dom/client';
import TrafficCityPlanner from './TrafficCityPlanner';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TrafficCityPlanner />
  </React.StrictMode>
);
